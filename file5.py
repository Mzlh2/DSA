#find the maximum element in an array
# arr = [12,17,8,9,0] 
# max_element = arr[0]
# for i in range(0, len(arr)-1):
#     if arr[i] > max_element:
#         max_element = arr[i]
# print(max_element)

#find the second maximum element in an array
'''import math
arr = [12,17,8,9,0] 
first_max = -math.inf
second_max = -math.inf
for curValue in arr:
    if curValue > first_max:
        second_max = first_max
        first_max = curValue
    elif curValue > second_max:
        second_max = curValue
print(second_max) '''
# T/C=O(n)
       